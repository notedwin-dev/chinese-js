// this is a function called 加 which takes two arguments and returns their sum
函数 加(a, b) {
  // translates to return a + b;
  返回 a + b;
}

函数 减(a, b) {
  返回 a - b;
}

// translates to let x = 1;
让 x = 1;

// runs console.log as usual JavaScript code
console.log("Hello World");

// translates to let y = 2;
让 y = 2;

// runs the function 加 with arguments x and y in JavaScript 
// should output 3
console.log(加(x, y));

// Should output -1
console.log(减(x, y));

/* expected output:
Hello World
3
3
*/